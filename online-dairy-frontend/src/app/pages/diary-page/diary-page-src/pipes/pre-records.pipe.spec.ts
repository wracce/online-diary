import { PreRecordsPipe } from './pre-records.pipe';

describe('PreRecordsPipe', () => {
  it('create an instance', () => {
    const pipe = new PreRecordsPipe();
    expect(pipe).toBeTruthy();
  });
});
